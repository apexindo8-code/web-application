<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Migrant Worker Digital Health Record System</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family:
          Segoe UI,
          Arial;
      }

      body {
        background: linear-gradient(135deg, #675dd4, #11a1f4);
        min-height: 100vh;
        color: white;
      }

      /* HERO IMAGE */
      .hero {
        height: 260px;
        background: url("https://images.unsplash.com/photo-1580281657527-47a2f2e7b60d?auto=format&fit=crop&w=1600&q=60")
          center/cover no-repeat;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
      }

      .hero h1 {
        background: rgba(0, 0, 0, 0.45);
        padding: 15px 25px;
        border-radius: 10px;
      }

      /* subtitle */
      .subtitle {
        text-align: center;
        color: #ffe0e0;
        margin: 20px;
      }

      /* dashboard layout */
      .dashboard {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
        padding: 40px;
      }

      .card {
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        border-radius: 15px;
        padding: 25px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        transition: 0.3s;
      }

      .card:hover {
        transform: translateY(-6px);
      }

      .card img {
        width: 100%;
        border-radius: 10px;
        margin-bottom: 15px;
      }

      /* headings */
      .card h3 {
        color: #fff176;
        margin-bottom: 10px;
      }

      /* form */
      label {
        color: #ffe082;
        font-weight: bold;
      }

      input {
        width: 100%;
        padding: 10px;
        margin: 8px 0 15px;
        border: none;
        border-radius: 6px;
      }

      /* button */
      button {
        width: 100%;
        padding: 12px;
        background: #00c9a7;
        border: none;
        border-radius: 6px;
        font-weight: bold;
        cursor: pointer;
        color: white;
      }

      button:hover {
        background: #009688;
      }

      /* search */
      .search {
        width: 60%;
        margin: 20px auto;
        display: block;
        padding: 10px;
        border-radius: 6px;
        border: none;
      }

      /* records */
      .records {
        padding: 20px 40px;
      }

      .record {
        background: white;
        color: black;
        padding: 15px;
        margin: 12px 0;
        border-radius: 8px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        animation: fade 0.4s ease;
      }

      .record b {
        color: #c62828;
      }

      .delete {
        float: right;
        background: #e53935;
        border: none;
        color: white;
        padding: 5px 10px;
        border-radius: 4px;
        cursor: pointer;
      }

      footer {
        text-align: center;
        padding: 20px;
        color: #ffe0e0;
        margin-top: 20px;
      }

      @keyframes fade {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @media (max-width: 900px) {
        .dashboard {
          grid-template-columns: 1fr;
        }
        .search {
          width: 80%;
        }
      }
    </style>
  </head>

  <body>
    <div class="hero">
      <h1>Portable Digital Health Record System for Migrant Workers</h1>
    </div>

    <div class="subtitle">
      Healthcare Initiative | Supporting SDG 3 – Good Health & Well-Being
    </div>

    <div class="dashboard">
      <div class="card">
        <img
          src="https://images.unsplash.com/photo-1581594693702-fbdc51b2763b?auto=format&fit=crop&w=800&q=60"
        />

        <h3>Add Health Record</h3>

        <label>Name</label>
        <input id="name" placeholder="Worker Name" />

        <label>Age</label>
        <input id="age" placeholder="Age" />

        <label>Location</label>
        <input id="location" placeholder="Work Location" />

        <label>Health Condition</label>
        <input id="condition" placeholder="Health Condition" />

        <button onclick="addRecord()">Save Record</button>
      </div>

      <div class="card">
        <img
          src="https://images.unsplash.com/photo-1584515933487-779824d29309?auto=format&fit=crop&w=800&q=60"
        />

        <h3>Why This System Matters</h3>

        <p style="line-height: 1.6">
          Migrant workers often lose their medical history when they move
          between jobs or regions. This digital platform allows clinics and
          healthcare workers to quickly access health records.

          <br /><br />

          Aligned with
          <b style="color: #fff176">Sustainable Development Goal 3</b> to ensure
          healthy lives and promote well-being for all.
        </p>
      </div>
    </div>

    <input
      class="search"
      placeholder="Search worker name..."
      onkeyup="searchRecord(this.value)"
    />

    <div class="records" id="records"></div>

    <footer>Migrant Worker Health Initiative | Digital Health Prototype</footer>

    <script>
      let records = JSON.parse(localStorage.getItem("healthRecords")) || [];

      function saveStorage() {
        localStorage.setItem("healthRecords", JSON.stringify(records));
      }

      function addRecord() {
        let name = document.getElementById("name").value;
        let age = document.getElementById("age").value;
        let location = document.getElementById("location").value;
        let condition = document.getElementById("condition").value;

        if (name == "" || age == "" || location == "" || condition == "") {
          alert("Please fill all fields");
          return;
        }

        let record = { name, age, location, condition };

        records.push(record);

        saveStorage();

        displayRecords();

        document.getElementById("name").value = "";
        document.getElementById("age").value = "";
        document.getElementById("location").value = "";
        document.getElementById("condition").value = "";
      }

      function displayRecords() {
        let container = document.getElementById("records");
        container.innerHTML = "";

        records.forEach((r, index) => {
          let div = document.createElement("div");
          div.className = "record";

          div.innerHTML = `
<button class="delete" onclick="deleteRecord(${index})">Delete</button>
<b>Name:</b> ${r.name}<br>
<b>Age:</b> ${r.age}<br>
<b>Location:</b> ${r.location}<br>
<b>Health Condition:</b> ${r.condition}
`;

          container.appendChild(div);
        });
      }

      function deleteRecord(i) {
        records.splice(i, 1);
        saveStorage();
        displayRecords();
      }

      function searchRecord(text) {
        let filtered = records.filter((r) =>
          r.name.toLowerCase().includes(text.toLowerCase()),
        );

        let container = document.getElementById("records");
        container.innerHTML = "";

        filtered.forEach((r) => {
          let div = document.createElement("div");
          div.className = "record";

          div.innerHTML = `
<b>Name:</b> ${r.name}<br>
<b>Age:</b> ${r.age}<br>
<b>Location:</b> ${r.location}<br>
<b>Health Condition:</b> ${r.condition}
`;

          container.appendChild(div);
        });
      }

      displayRecords();
    </script>
  </body>
  <section
    style="padding: 40px; max-width: 900px; margin: auto; line-height: 1.7"
  >
    <h2 style="color: #fff176; text-align: center; margin-bottom: 20px">
      Challenges Faced by Migrant Workers
    </h2>

    <p>
      Migrant workers in Kerala often travel long distances in search of
      employment in construction, manufacturing, and service industries. Many of
      these workers face difficult living conditions, limited access to
      healthcare facilities, and language barriers that make communication with
      doctors difficult. Because they frequently move between workplaces and
      regions, their medical records are usually kept on paper or not recorded
      at all. As a result, important health information such as previous
      illnesses, allergies, or treatments can easily be lost.
    </p>

    <p>
      This lack of accessible medical records creates serious problems when
      workers fall ill or experience accidents. Doctors may not have access to
      the worker's medical history, which can delay treatment and increase
      health risks. In many cases, migrant workers also struggle to receive
      consistent medical care because their health information is scattered or
      unavailable across different hospitals and clinics.
    </p>

    <h2 style="color: #fff176; text-align: center; margin: 30px 0 20px">
      Digital Solution for Healthcare Access
    </h2>

    <p>
      The Digital Health Record Management System provides a simple and
      accessible platform where migrant workers’ health information can be
      securely stored and managed. Through this website, healthcare providers
      can record essential medical details such as personal information, health
      conditions, and treatment history. This ensures that the worker’s health
      data is available whenever it is needed, regardless of where the worker
      moves for employment.
    </p>

    <p>
      By digitizing health records, this system supports faster medical
      diagnosis, better continuity of care, and improved communication between
      workers and healthcare professionals. The platform also contributes to the
      goals of global healthcare improvement by supporting initiatives such as
      Sustainable Development Goal 3, which focuses on ensuring healthy lives
      and promoting well-being for all people.
    </p>
  </section>
</html>
